<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=1080, height=1920">
  <title>Marathon Giảm Cân - Daily Leaderboard</title>

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      width: 1080px;
      min-height: 1920px;
      font-family: 'Inter', system-ui, Arial, sans-serif;
      background: linear-gradient(
        to bottom,
        #9C27B0 0%,
        #7B1FA2 55%,
        #4A148C 100%
      );
      padding: 48px 60px 0 60px; /* ⬅ giảm ~20% */
      display: flex;
      flex-direction: column;
    }

    /* ================= HEADER ================= */

    .header {
      text-align: center;
      margin-bottom: 22px;
    }

    .line1-small {
      font-size: 28px;
      font-weight: 600;
      color: #FFFFFF;
      margin-bottom: 8px;
    }

    .title-big {
      font-size: 74px;
      font-weight: 800;
      color: #FFFFFF;
      margin-bottom: 28px;
      line-height: 1.05;
    }

    .team-name {
      font-size: 44px;
      font-weight: 700;
      color: #FFFFFF;
      margin-bottom: 16px;
    }

    .meta {
      font-size: 28px;
      font-weight: 500;
      color: rgba(255, 255, 255, 0.85);
      margin-bottom: 22px;
    }

    /* ================= COLUMN LABEL ================= */

    .col-labels {
      display: flex;
      justify-content: flex-end;
      gap: 140px;
      margin-bottom: 14px;
      padding-right: 40px;
    }

    .col-label {
      font-size: 30px;
      font-weight: 700;
      color: #FFFFFF;
      text-align: center;
      width: 150px;
    }

    /* ================= BODY ================= */

    .leaderboard {
      display: flex;
      flex-direction: column;
      gap: 18px;
      flex: 1;
      justify-content: space-evenly;
    }

    .player-row {
      width: 100%;
      height: 112px; /* ⬅ giảm từ 132 */
      background: #E7E6FF;
      border-radius: 42px;
      box-shadow: 0 6px 18px rgba(0, 0, 0, 0.18);
      display: flex;
      align-items: center;
      padding: 0 36px;
      position: relative;
    }

    .player-row.winner {
      background: #7CF08A;
      border: 8px solid #FFFFFF;
      box-shadow: 0 8px 22px rgba(0, 0, 0, 0.22);
      padding: 0 28px;
    }

    /* ================= AVATAR ================= */

    .avatar-oval {
      width: 168px;
      height: 128px;
      background: rgba(255, 255, 255, 0.55);
      border-radius: 54px;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
      position: relative;
    }

    .avatar {
      width: 104px;
      height: 104px;
      border-radius: 50%;
      object-fit: cover;
      border: 4px solid rgba(255, 255, 255, 0.7);
    }

    .crown-icon {
      width: 70px;
      height: 70px;
      position: absolute;
      z-index: 10;
      pointer-events: none;
    }

    /* ================= TEXT ================= */

    .player-name {
      margin-left: 56px;
      font-size: 34px;
      font-weight: 600;
      color: #1F2A37;
      flex: 1;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }

    .value-container {
      display: flex;
      align-items: center;
      gap: 12px;
      width: 150px;
      justify-content: flex-end;
    }

    .value-today {
      font-size: 46px;
      font-weight: 800;
      color: #FF7A00;
      text-align: right;
    }

    .value-round {
      font-size: 46px;
      font-weight: 800;
      color: #5B3BFF;
      text-align: right;
    }

    .captain-text {
      font-size: 34px;
      font-weight: 600;
      color: rgba(31, 42, 55, 0.65);
      text-align: right;
    }

    /* ================= FOOTER ================= */

    .footer-bar {
      width: 1080px;
      height: 176px; /* ⬅ giảm từ 220 */
      background: rgba(40, 76, 208, 0.55);
      display: flex;
      align-items: center;
      justify-content: center;
      margin-left: -60px;
      margin-top: auto;
    }

    .footer-text {
      font-size: 52px;
      font-weight: 800;
      color: #FFFFFF;
      text-align: center;
    }
  </style>
</head>

<body>

  <!-- ================= HEADER ================= -->
  <div class="header">
    <div class="line1-small">nexme marathon</div>
    <div class="title-big">Marathon Giảm Cân</div>
    <div class="team-name">{{team_name}}</div>
    <div class="meta">Vòng {{round_number}}, ngày {{day_number}}</div>
  </div>

  <!-- ================= COLUMN LABEL ================= -->
  <div class="col-labels">
    <div class="col-label">Hôm nay</div>
    <div class="col-label">vòng này</div>
  </div>

  <!-- ================= LEADERBOARD ================= -->
  <div class="leaderboard">
    {{#each players}}
      {{#if is_captain}}

        <!-- CAPTAIN ROW -->
        <div class="player-row">
          <div class="avatar-oval">
            <img src="{{avatar}}" class="avatar">
          </div>

          <div class="player-name">{{name}}</div>

          <div class="value-container" style="width: 300px;">
            <div class="captain-text">Đội Trưởng</div>
          </div>
        </div>

      {{else}}

        <!-- PLAYER ROW -->
        <div class="player-row {{#if (eq rank "1")}}winner{{/if}}">
          <div class="avatar-oval">
            <img src="{{avatar}}" class="avatar">
          </div>

          <div class="player-name">{{name}}</div>

          <div class="value-container">
            <div class="value-today">{{today_display}}</div>
          </div>

          <div class="value-container">
            <div class="value-round">{{round_display}}</div>
          </div>

          {{#if (eq rank "1")}}
            <img
              src="https://raw.githubusercontent.com/beanbean/nexme-render-templates/main/crown.svg"
              class="crown-icon"
              style="top:-70px; left:180px;"
            >
          {{/if}}
        </div>

      {{/if}}
    {{/each}}
  </div>

  <!-- ================= FOOTER ================= -->
  <div class="footer-bar">
    <div class="footer-text">
      ĐỘI GIẢM: {{team_today_loss}}kg{{#if team_all_gain}} ⚠️{{/if}}
    </div>
  </div>

</body>
</html>
