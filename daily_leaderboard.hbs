<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=1080, height=1920">
  <title>Marathon Giảm Cân - Daily Leaderboard</title>

  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      width: 1080px;
      height: 1920px;
      font-family: 'Inter', system-ui, Arial, sans-serif;
      background: linear-gradient(to bottom, #9C27B0 0%, #7B1FA2 55%, #4A148C 100%);
      padding: 48px 60px 0 60px;
      display: flex;
      flex-direction: column;
    }

    /* ================= HEADER ================= */
    .header {
      text-align: center;
      margin-bottom: 22px;
    }

    .line1-small {
      font-size: 28px;
      font-weight: 600;
      color: #FFFFFF;
      margin-bottom: 6px;
    }

    .title-big {
      font-size: 74px;
      font-weight: 800;
      color: #FFFFFF;
      margin-bottom: 18px;
      line-height: 1.1;
    }

    .team-name {
      font-size: 44px;
      font-weight: 700;
      color: #FFFFFF;
      margin-bottom: 10px;
    }

    .meta {
      font-size: 28px;
      font-weight: 500;
      color: rgba(255,255,255,0.85);
    }

    /* ================= COLUMN LABELS ================= */
    .col-labels {
      display: flex;
      justify-content: flex-end;
      gap: 38px;
      margin: 14px 0;
      padding-right: 40px;
    }

    .col-label {
      font-size: 28px;
      font-weight: 700;
      color: #FFFFFF;
      width: 140px;
      text-align: center;
    }

    /* ================= LEADERBOARD ================= */
    .leaderboard {
      display: flex;
      flex-direction: column;
      gap: 18px;
      flex: 1;
    }

    .player-row {
      height: 122px;
      background: #E7E6FF;
      border-radius: 42px;
      box-shadow: 0 5px 14px rgba(0,0,0,0.18);
      display: flex;
      align-items: center;
      padding: 0 36px;
      position: relative;
    }

    .player-row.winner {
      background: #7CF08A;
      border: 8px solid #FFFFFF;
      padding: 0 28px;
    }

    /* KHUNG AVATAR - Cần position: relative để neo vương miện */
    .avatar-oval {
      width: 168px;
      height: 124px;
      background: rgba(255,255,255,0.55);
      border-radius: 52px;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
      position: relative; /* QUAN TRỌNG: Để vương miện căn theo khung này */
    }

    .avatar {
      width: 104px;
      height: 104px;
      border-radius: 50%;
      object-fit: cover;
      border: 4px solid rgba(255,255,255,0.7);
    }

    /* CSS VƯƠNG MIỆN */
    .crown-icon {
      width: 90px;
      height: 90px;
      position: absolute;
      /* Đẩy lên trên đầu avatar */
      top: -58px;
      /* Căn giữa theo chiều ngang của .avatar-oval */
      left: 50%;
      transform: translateX(-50%);
      z-index: 20;
    }

    .player-name {
      margin-left: 52px;
      font-size: 34px;
      font-weight: 600;
      color: #1F2A37;
      flex: 1;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    .value-container {
      width: 140px;
      display: flex;
      justify-content: flex-end;
      align-items: center;
      gap: 10px;
    }

    .value-today {
      font-size: 44px;
      font-weight: 800;
      color: #FF7A00;
    }

    .value-round {
      font-size: 44px;
      font-weight: 800;
      color: #5B3BFF;
    }

    .captain-text {
      font-size: 32px;
      font-weight: 700;
      color: rgba(31,42,55,0.7);
    }

    /* ================= FOOTER ================= */
    .footer-bar {
      height: 176px;
      background: linear-gradient(
        to right,
        #6A1B9A,
        #7B1FA2,
        #6A1B9A
      );
      margin-left: -60px;
      margin-right: -60px;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .footer-text {
      font-size: 54px;
      font-weight: 800;
      color: #FFFFFF;
    }
  </style>
</head>

<body>

  <div class="header">
    <div class="line1-small">nexme marathon</div>
    <div class="title-big">Marathon Giảm Cân</div>
    <div class="team-name">{{team_name}}</div>
    <div class="meta">Vòng {{round_number}}, ngày {{day_number}}</div>
  </div>

  <div class="col-labels">
    <div class="col-label">Hôm nay</div>
    <div class="col-label">Vòng này</div>
  </div>

  <div class="leaderboard">
    {{#each players}}
      {{#if is_captain}}
        <!-- ĐỘI TRƯỞNG -->
        <div class="player-row">
          <div class="avatar-oval">
            <img src="{{avatar}}" class="avatar">
          </div>
          <div class="player-name">{{name}}</div>
          <div class="value-container" style="width:300px;">
            <div class="captain-text">ĐỘI TRƯỞNG</div>
          </div>
        </div>
      {{else}}
        <!-- THÀNH VIÊN -->
        <div class="player-row {{#if is_top}}winner{{/if}}">

          <!-- KHUNG AVATAR (Chứa cả ảnh và vương miện) -->
          <div class="avatar-oval">
            {{#if is_top}}
              <img
                src="https://raw.githubusercontent.com/beanbean/nexme-render-templates/main/crown.svg"
                class="crown-icon"
              >
            {{/if}}
            <img src="{{avatar}}" class="avatar">
          </div>

          <div class="player-name">{{name}}</div>

          <div class="value-container">
            <div class="value-today">{{today_display}}</div>
          </div>

          <div class="value-container">
            <div class="value-round">{{round_display}}</div>
          </div>

        </div>
      {{/if}}
    {{/each}}
  </div>

  <div class="footer-bar">
    <div class="footer-text">
      Đội hôm nay giảm: {{#if team_today_loss}}{{team_today_loss}}{{else}}0{{/if}}kg
    </div>
  </div>

</body>
</html>
