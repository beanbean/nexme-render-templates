<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=1080, height=1920">
  <title>Marathon Giảm Cân - Weight Leaderboard</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      width: 1080px;
      min-height: 1920px;
      font-family: 'Inter', system-ui, Arial, sans-serif;
      background: linear-gradient(to bottom, #14C7D5 0%, #4AA6D9 55%, #3B5BD6 100%);
      padding: 60px 60px 0 60px;
      display: flex;
      flex-direction: column;
    }

    .header {
      text-align: center;
      margin-bottom: 30px;
    }

    .line1-small {
      font-size: 34px;
      font-weight: 600;
      color: #FFFFFF;
      margin-bottom: 10px;
    }

    .title-big {
      font-size: 92px;
      font-weight: 800;
      color: #FFFFFF;
      margin-bottom: 40px;
      line-height: 1.1;
    }

    .team-name {
      font-size: 54px;
      font-weight: 700;
      color: #FFFFFF;
      margin-bottom: 20px;
    }

    .meta {
      font-size: 34px;
      font-weight: 500;
      color: rgba(255, 255, 255, 0.85);
      margin-bottom: 30px;
    }

    .col-labels {
      display: flex;
      justify-content: flex-end;
      gap: 160px;
      margin-bottom: 16px;
      padding-right: 40px; /* P3: Adjusted to center labels over columns */
    }

    .col-label {
      font-size: 34px;
      font-weight: 700;
      color: #FFFFFF;
      text-align: center;
      width: 160px;
    }

    .leaderboard {
      display: flex;
      flex-direction: column;
      gap: 22px;
      flex: 1;
    }

    .player-row {
      width: 100%;
      height: 132px;
      background: #E7E6FF;
      border-radius: 48px;
      box-shadow: 0 6px 18px rgba(0, 0, 0, 0.18);
      display: flex;
      align-items: center;
      padding: 0 40px;
      position: relative;
    }

    .player-row.winner {
      background: #7CF08A;
      border: 10px solid #FFFFFF;
      box-shadow: 0 8px 22px rgba(0, 0, 0, 0.22);
      padding: 0 30px;
    }

    .avatar-oval {
      width: 192px;  /* P4: Increased from 160px (20% larger) */
      height: 144px; /* P4: Increased from 120px (20% larger) */
      background: rgba(255, 255, 255, 0.55);
      border-radius: 60px; /* P4: Scaled from 50px */
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
      position: relative;
    }

    .avatar {
      width: 120px;  /* P4: Increased from 100px (20% larger) */
      height: 120px;
      border-radius: 50%;
      object-fit: cover;
      border: 4px solid rgba(255, 255, 255, 0.7);
    }

    .crown-icon {
      width: 78px;
      height: 78px;
      position: absolute;  /* P1: Absolute positioning for z-index control */
      z-index: 9999;       /* P1: Ensure crown is on top layer */
      pointer-events: none; /* P1: Don't interfere with interactions */
    }

    .player-name {
      margin-left: 70px;
      font-size: 38px;
      font-weight: 600;
      color: #1F2A37;
      flex: 1;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }

    .value-container {
      display: flex;
      align-items: center;
      gap: 14px;
      width: 160px;
      justify-content: flex-end;
    }

    .value-today {
      font-size: 52px;
      font-weight: 800;
      color: #FF7A00;
      text-align: right;
    }

    .value-round {
      font-size: 52px;
      font-weight: 800;
      color: #5B3BFF;
      text-align: right;
    }

    .warning-icon {
      width: 74px;
      height: 74px;
      flex-shrink: 0;
    }

    .captain-text {
      font-size: 36px;
      font-weight: 600;
      color: rgba(31, 42, 55, 0.65); /* P2: Darker for better readability */
      text-align: right;
      width: 160px;
      margin-left: 14px; /* P2: Match value-container gap */
    }

    .footer-bar {
      width: 1080px;
      height: 220px;
      background: rgba(40, 76, 208, 0.55);
      display: flex;
      align-items: center;
      justify-content: center;
      margin-left: -60px;
      margin-top: auto;
    }

    .footer-text {
      font-size: 64px;
      font-weight: 800;
      color: #FFFFFF;
      text-align: center;
    }
  </style>
</head>
<body>
  <!-- Header -->
  <div class="header">
    <div class="line1-small">nexme marathon</div>
    <div class="title-big">Marathon Giảm Cân</div>
    <div class="team-name">{{team_name}}</div>
    <div class="meta">Vòng {{round_number}}, ngày {{day_number}}</div>
  </div>

  <!-- Column Labels -->
  <div class="col-labels">
    <div class="col-label">Hôm nay</div>
    <div class="col-label">vòng này</div>
  </div>

  <!-- Leaderboard -->
  <div class="leaderboard">
    {{#each players}}
      {{#if is_captain}}
        <!-- P2: Captain Row (always last) -->
        <div class="player-row">
          <div class="avatar-oval">
            <img src="{{avatar}}" alt="{{name}}" class="avatar">
          </div>
          <div class="player-name">{{name}}</div>
          <div class="captain-text">Đội Trưởng</div>
          <div class="captain-text">Đội Trưởng</div>
        </div>
      {{else}}
        <!-- Player Row -->
        <div class="player-row {{#if (eq rank "1")}}winner{{/if}}" style="position: relative;">
          <div class="avatar-oval">
            <img src="{{avatar}}" alt="{{name}}" class="avatar">
          </div>

          <div class="player-name">{{name}}</div>

          <!-- Today Value -->
          <div class="value-container">
            {{#if has_warning_today}}
              <img src="https://raw.githubusercontent.com/beanbean/nexme-render-templates/main/warning.svg" alt="Warning" class="warning-icon">
            {{/if}}
            <div class="value-today">{{today_display}}</div>
          </div>

          <!-- Round Value -->
          <div class="value-container">
            <div class="value-round">{{round_display}}</div>
          </div>

          <!-- P1: Crown rendered AFTER row content for proper z-index layering -->
          {{#if (eq rank "1")}}
            <img src="https://raw.githubusercontent.com/beanbean/nexme-render-templates/main/crown.svg" alt="Crown" class="crown-icon" style="top: -78px; left: 195px;">
          {{/if}}
        </div>
      {{/if}}
    {{/each}}
  </div>

  <!-- Footer -->
  <div class="footer-bar">
    <div class="footer-text">
      Đội hôm nay giảm: {{team_today_loss}}kg{{#if team_all_gain}} ⚠️{{/if}}
    </div>
  </div>
</body>
</html>
