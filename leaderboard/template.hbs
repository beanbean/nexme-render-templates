<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=1080, initial-scale=1.0">
    <title>Leaderboard</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700;900&display=swap" rel="stylesheet">
    <style>
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body { width: 1080px; height: 1600px; background: #DCDDE6; font-family: 'Roboto', sans-serif; display: flex; justify-content: center; align-items: center; }
        
        .card { 
            width: 980px; height: 1500px; 
            background: #F0F0F3; border-radius: 40px; padding: 40px; 
            box-shadow: -10px -10px 30px #FFFFFF, 10px 10px 30px rgba(174, 174, 192, 0.4); 
            display: flex; flex-direction: column; 
        }
        
        /* HEADER */
        .header { text-align: center; margin-bottom: 30px; }
        .title { font-size: 32px; color: #888; letter-spacing: 2px; text-transform: uppercase; }
        .team-name { font-size: 60px; font-weight: 900; margin: 10px 0; text-transform: uppercase; color: #E64A19; }
        .date { font-size: 28px; color: #7CB342; font-weight: 500; }

        /* LIST */
        .list { flex: 1; display: flex; flex-direction: column; gap: 20px; overflow: hidden; }
        .item { 
            display: flex; align-items: center; 
            background: #F0F0F3; padding: 20px 30px; 
            border-radius: 25px; 
            box-shadow: 6px 6px 12px rgba(163,177,198, 0.3), -6px -6px 12px #FFFFFF; 
            border-left: 10px solid transparent;
        }
        
        /* Rank Styles */
        .rank { font-size: 36px; font-weight: 900; color: #888; width: 60px; text-align: center; margin-right: 10px; }
        
        /* Top 3 Colors */
        .item.top-1 { border-left-color: #FFD700; background: linear-gradient(90deg, #fff9c4 0%, #F0F0F3 100%); }
        .item.top-2 { border-left-color: #C0C0C0; }
        .item.top-3 { border-left-color: #CD7F32; }
        .top-1 .rank { color: #FFBC00; font-size: 48px; }

        /* Avatar */
        .avatar-container { position: relative; margin-right: 25px; }
        .avatar { 
            width: 90px; height: 90px; border-radius: 50%; 
            object-fit: cover; border: 4px solid #fff; 
            box-shadow: 0 4px 8px rgba(0,0,0,0.1); 
        }
        .crown {
            position: absolute; top: -25px; left: 50%; transform: translateX(-50%);
            font-size: 40px; text-shadow: 0 2px 4px rgba(0,0,0,0.2);
            animation: float 2s ease-in-out infinite;
        }

        /* Info */
        .info { flex: 1; }
        .name { font-size: 34px; color: #444; font-weight: 700; display: flex; align-items: center; }
        .me-tag { 
            font-size: 18px; background: #7CB342; color: white; 
            padding: 2px 10px; border-radius: 10px; margin-left: 10px; font-weight: 500; 
        }
        
        /* Stats */
        .stats { text-align: right; }
        .lost { font-size: 38px; color: #E64A19; font-weight: 900; }
        .points { font-size: 22px; color: #888; }

        /* FOOTER */
        .footer { 
            margin-top: 30px; padding-top: 20px;
            border-top: 2px dashed #ccc;
            text-align: center; color: #555; font-size: 28px; font-weight: 500; 
        }
        .footer strong { color: #7CB342; font-weight: 900; font-size: 32px; }

        @keyframes float { 0%, 100% { transform: translate(-50%, 0); } 50% { transform: translate(-50%, -5px); } }
    </style>
</head>
<body>
    <div class="card">
        <div class="header">
            <div class="title">BẢNG XẾP HẠNG NGÀY {{round_config.day_index}}</div>
            <div class="team-name">{{player.team}}</div>
            <div class="date">Round: {{round_config.name}}</div>
        </div>

        <div class="list">
            {{#each leaderboard}}
            <div class="item {{#if (eq @index 0)}}top-1{{/if}} {{#if (eq @index 1)}}top-2{{/if}} {{#if (eq @index 2)}}top-3{{/if}}">
                
                <!-- 1. RANK -->
                <div class="rank">#{{add @index 1}}</div>
                
                <!-- 2. AVATAR & CROWN -->
                <div class="avatar-container">
                    {{#if (eq @index 0)}}<div class="crown">👑</div>{{/if}}
                    <!-- Dùng fallback UI Avatar nếu không có ảnh thật -->
                    <img src="{{#if avatar}}{{avatar}}{{else}}https://ui-avatars.com/api/?name={{name}}&background=random&size=128{{/if}}" class="avatar">
                </div>

                <!-- 3. NAME -->
                <div class="info">
                    <div class="name">
                        {{name}}
                        {{#if is_me}}<span class="me-tag">Bạn</span>{{/if}}
                    </div>
                </div>

                <!-- 4. STATS -->
                <div class="stats">
                    <div class="lost">-{{total_lost}} kg</div>
                    <div class="points">{{points}} điểm</div>
                </div>
            </div>
            {{/each}}
        </div>

        <div class="footer">
            Hôm nay đội mình giảm trung bình: <strong>{{team_health.avg_weight_lost}} kg/người</strong>
        </div>
    </div>
</body>
</html>
