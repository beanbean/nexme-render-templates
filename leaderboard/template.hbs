<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=1080, initial-scale=1.0">
    <title>Leaderboard</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700;900&display=swap" rel="stylesheet">
    <style>
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body { width: 1080px; height: 1600px; background: #DCDDE6; font-family: 'Roboto', sans-serif; display: flex; justify-content: center; align-items: center; }
        
        .card { 
            width: 980px; height: 1500px; 
            background: #F0F0F3; border-radius: 40px; padding: 40px; 
            box-shadow: -10px -10px 30px #FFFFFF, 10px 10px 30px rgba(174, 174, 192, 0.4); 
            display: flex; flex-direction: column; 
        }
        
        /* HEADER */
        .header { text-align: center; margin-bottom: 30px; }
        .title { font-size: 32px; color: #888; letter-spacing: 2px; text-transform: uppercase; }
        .team-name { font-size: 60px; font-weight: 900; margin: 10px 0; text-transform: uppercase; color: #E64A19; }
        .date { font-size: 28px; color: #7CB342; font-weight: 500; }

        /* LIST */
        .list { flex: 1; display: flex; flex-direction: column; gap: 20px; overflow: hidden; }
        .item { 
            display: flex; align-items: center; 
            background: #F0F0F3; padding: 15px 30px; /* Giảm padding một chút cho gọn */
            border-radius: 25px; 
            box-shadow: 6px 6px 12px rgba(163,177,198, 0.3), -6px -6px 12px #FFFFFF; 
            border-left: 10px solid transparent;
        }
        
        /* Top 3 Colors */
        .item.top-1 { border-left-color: #FFD700; background: linear-gradient(90deg, #fffde7 0%, #F0F0F3 100%); }
        .item.top-2 { border-left-color: #C0C0C0; }
        .item.top-3 { border-left-color: #CD7F32; }

        /* 1. RANK */
        .rank { font-size: 36px; font-weight: 900; color: #888; width: 50px; text-align: center; margin-right: 15px; }
        .top-1 .rank { color: #FFBC00; font-size: 48px; }

        /* 2. AVATAR (Tròn, nổi) */
        .avatar { 
            width: 80px; height: 80px; border-radius: 50%; 
            object-fit: cover; border: 3px solid #fff; 
            box-shadow: 0 4px 6px rgba(0,0,0,0.1); 
            margin-right: 15px;
        }

        /* 3. BADGES (Khu vực chứa Icon Vương miện...) */
        .badges {
            display: flex; align-items: center; gap: 10px;
            margin-right: 15px;
            min-width: 0; /* Để flex không bị vỡ */
        }
        .icon-badge {
            width: 45px; height: 45px; object-fit: contain;
            filter: drop-shadow(0 2px 2px rgba(0,0,0,0.2));
            animation: bounce 2s infinite ease-in-out;
        }

        /* 4. INFO (Tên) */
        .info { flex: 1; display: flex; flex-direction: column; justify-content: center; }
        .name { font-size: 32px; color: #444; font-weight: 700; line-height: 1.2; }
        .me-tag { 
            display: inline-block; font-size: 16px; background: #7CB342; color: white; 
            padding: 2px 8px; border-radius: 8px; margin-left: 8px; vertical-align: middle; 
            font-weight: 500; transform: translateY(-2px);
        }

        /* 5. STATS */
        .stats { text-align: right; min-width: 140px; }
        .lost { font-size: 38px; color: #E64A19; font-weight: 900; }
        .points { font-size: 20px; color: #888; margin-top: 2px; }

        /* FOOTER */
        .footer { 
            margin-top: 20px; padding-top: 20px;
            border-top: 2px dashed #ccc;
            text-align: center; color: #555; font-size: 28px; font-weight: 500; 
        }
        .footer strong { color: #7CB342; font-weight: 900; font-size: 32px; }

        @keyframes bounce { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(-5px); } }
    </style>
</head>
<body>
    <div class="card">
        <div class="header">
            <div class="title">BẢNG XẾP HẠNG NGÀY {{round_config.day_index}}</div>
            <div class="team-name">{{player.team}}</div>
            <div class="date">Round: {{round_config.name}}</div>
        </div>

        <div class="list">
            {{#each leaderboard}}
            <div class="item {{#if (eq @index 0)}}top-1{{/if}} {{#if (eq @index 1)}}top-2{{/if}} {{#if (eq @index 2)}}top-3{{/if}}">
                
                <!-- 1. RANK -->
                <div class="rank">#{{add @index 1}}</div>
                
                <!-- 2. AVATAR -->
                <img src="{{#if avatar}}{{avatar}}{{else}}https://ui-avatars.com/api/?name={{name}}&background=random&size=128{{/if}}" class="avatar">

                <!-- 3. BADGES AREA (Giữa Avatar & Tên) -->
                <div class="badges">
                    <!-- Top 1: Vương miện -->
                    {{#if (eq @index 0)}}
                        <img src="https://img.icons8.com/fluency/96/crown.png" class="icon-badge">
                    {{/if}}
                    
                    <!-- Sau này thêm icon khác vào đây:
                    <img src="..." class="icon-badge"> 
                    -->
                </div>

                <!-- 4. NAME -->
                <div class="info">
                    <div class="name">
                        {{name}}
                        {{#if is_me}}<span class="me-tag">Bạn</span>{{/if}}
                    </div>
                </div>

                <!-- 5. STATS -->
                <div class="stats">
                    <div class="lost">-{{total_lost}} kg</div>
                    <div class="points">{{points}} điểm</div>
                </div>
            </div>
            {{/each}}
        </div>

        <div class="footer">
            Tổng cân nặng đội mình giảm: <strong>{{team_health.total_weight_lost}} kg</strong>
        </div>
    </div>
</body>
</html>
